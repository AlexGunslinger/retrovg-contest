.page-header
  %h1 Main Scoreboard

%table.table.table-striped.table-hover
  %thead
    %tr
      %th Pos
      %th Player
      %th Score
  %tbody
    - @players.each_with_index do |p, i|
      %tr
        %td
          = i + 1
        %td 
          = p[:name]
        %td 
          = p[:points]